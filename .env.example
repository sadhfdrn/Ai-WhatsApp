# WhatsApp Credentials (base64 encoded JSON)
WHATSAPP_CREDS=

# Server Port (optional, defaults to 8080)
PORT=8080

# Command Prefix (optional, defaults to ".")
# Examples: PREFIX=. or PREFIX=/ or PREFIX=! or PREFIX=null (for no prefix)
PREFIX=.